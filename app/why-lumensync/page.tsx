import { PageScaffold, pageMetadata } from "@/components/page-scaffold";

export const metadata = pageMetadata("/why-lumensync");

export default function Page() {
  return <PageScaffold path="/why-lumensync" eyebrow="Why LumenSync" />;
}
